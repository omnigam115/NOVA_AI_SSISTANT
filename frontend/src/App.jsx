import AppRouter from './router/AppRouter'

/**
 * Root application component. Kept intentionally thin — its only
 * responsibility is mounting the router.
 */
function App() {
  return <AppRouter />
}

export default App
